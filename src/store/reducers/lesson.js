const initialState = {
    lessons:[],
    loading: false,
    lesson: null
}

export default function lessonReducer(state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}